import type { Config } from "tailwindcss";

export default {
  darkMode: "class",
  theme: {
    extend: {
      fontFamily: {
        "hind-madurai": ["HindMadurai", "serif"],
      },
    },
  },
} satisfies Config;
